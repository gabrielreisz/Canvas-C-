#include "Canvas.hpp" // Supondo que seu TAD esteja em canvas . hpp

int main(void)
{
    // Declaracao do canvas principal
    Canva tela;

    // PASSO 1: Criar e exibir um canvas vazio
    tela.CriarCanva(tela, 40, 12);
    tela.ImprimirCanva(tela);
}